<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <!--toolbar标题文字颜色-->
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="toolbar_title_text" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!--修改toolbar的Title颜色.正确-->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!--修改toolbar的subtitle(小标题)颜色.正确-->
        <item name="subtitleTextColor">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="toolbar_theme" parent="AppTheme.AppBarOverlay">
        <item name="actionMenuTextColor">@android:color/white</item> <!--颜色-->
        <item name="android:textSize">18sp</item> <!--字体大小-->
    </style>
</resources>
